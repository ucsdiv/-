# DeepSeek Coder Mobile - 移动端依赖
# 针对手机端优化的最小依赖集
#
# 安装: pip install -r requirements_mobile.txt

# ============================================================
# 核心依赖 (必需)
# ============================================================
torch>=1.13.0,<3.0.0
tokenizers>=0.14.0,<1.0.0

# ============================================================
# 推理依赖 (推荐)
# ============================================================
transformers>=4.30.0,<5.0.0

# ============================================================
# 可选依赖 (按需安装)
# ============================================================
# 内存监控
psutil>=5.9.0

# 量化模型保存 (safetensors 格式)
safetensors>=0.4.0

# 模型下载
huggingface-hub>=0.16.0
# modelscope>=1.9.0  # 国内用户可用 ModelScope 加速

# ============================================================
# 注意:
# 1. 手机端建议使用 CPU 版 PyTorch:
#    pip install torch --extra-index-url https://download.pytorch.org/whl/cpu
# 2. 内存紧张时可只安装 torch 和 tokenizers
# 3. Termux 用户可使用 pkg 安装预编译包:
#    pkg install python python-dev
# 4. 国内用户可使用清华镜像源加速:
#    pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements_mobile.txt
# ============================================================
